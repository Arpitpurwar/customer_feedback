export interface Configuration {

    projectId: string;
    projectName: string;
    date: string;
    team: string;
    description: string;
}

export interface Team {
    id: string;
    name: string;
}
